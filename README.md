# tarkov-korea-wiki-back

pip install 'fastapi[all]'

pip install sqlalchemy

pip install python-dotenv

pip install psycopg2-binary

pip install requests

pip install pytz

pip install aiokafka

uvicorn main:app --reload